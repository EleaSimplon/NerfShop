{% extends 'base.html.twig' %}

{% block title %}AdresseLivraison index{% endblock %}

{% block body %}
    <h1>AdresseLivraison index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Adresse</th>
                <th>CompAdresse</th>
                <th>Ville</th>
                <th>CodePostal</th>
                <th>Pays</th>
                <th>Telephone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for adresse_livraison in adresse_livraisons %}
            <tr>
                <td>{{ adresse_livraison.id }}</td>
                <td>{{ adresse_livraison.nom }}</td>
                <td>{{ adresse_livraison.prenom }}</td>
                <td>{{ adresse_livraison.adresse }}</td>
                <td>{{ adresse_livraison.compAdresse }}</td>
                <td>{{ adresse_livraison.ville }}</td>
                <td>{{ adresse_livraison.codePostal }}</td>
                <td>{{ adresse_livraison.pays }}</td>
                <td>{{ adresse_livraison.telephone }}</td>
                <td>
                    <a href="{{ path('adresse_livraison_show', {'id': adresse_livraison.id}) }}">show</a>
                    <a href="{{ path('adresse_livraison_edit', {'id': adresse_livraison.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('adresse_livraison_new') }}">Create new</a>
{% endblock %}
